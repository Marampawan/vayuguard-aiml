services:
  - type: web
    name: vayuguard-ml
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/api/main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0